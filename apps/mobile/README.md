# Mobile App

Expo React Native app for Grocery Pal.

The Convex backend has moved to `packages/backend`.

Run from workspace root:

```bash
bun run dev
```

Or run directly in this package:

```bash
bun run start
```
